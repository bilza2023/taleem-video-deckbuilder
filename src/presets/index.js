import { textPresets } from './text.js';
import { imagePresets } from './image.js';
// import { iconPresets } from './icon.js';

export const presets = {
  text: textPresets,
  image: imagePresets,
//   icon: iconPresets
};
