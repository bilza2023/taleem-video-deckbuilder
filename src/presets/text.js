// presets/text.js
export const textPresets = {
    title: {
      fontSize: 42,
      fontWeight: "bold",
      fill: "#ffffff", // placeholder, replace with theme.primary
      align: "center"
    },
    body: {
      fontSize: 32,
      fontWeight: "normal",
      fill: "#dddddd", // placeholder, replace with theme.text
      align: "left"
    },
    caption: {
      fontSize: 28,
      fontStyle: "italic",
      fill: "#999999", // placeholder, replace with theme.dim
      align: "center"
    }
  };
  